<!DOCTYPE html>
<html>
  <head>
    <title>BlogOnRails</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
      <header>
      <nav class="navbar navbar-light bg-light">
        <div class="nav-container"> 
          <a class="text-primary navbar-brand" href="<%= root_path %>" style="margin-left: 1em">Blog on Rails</a>
        </div>
        <div class="nav-items-set justify-content-end">
          <ul class="nav">

            <li class="nav-item active">
              <a class="text-primary nav-link" href="<%= root_path %>">Home</a>
            </li>
            <li class="nav-item">
              <a class="text-primary nav-link" href="<%= new_post_path %>">New Post</a>
            </li>
          </ul>
        </div>
      </nav>
    </header> 
    <div>
      <% flash.each do |type, message| %>
        <div class="alert alert-info" role="alert">
          <%= type + "------" + message %>
        </div>
      <% end %>
    </div>
    <%= yield %>
  </body>
</html>
